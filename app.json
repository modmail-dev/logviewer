{
  "name": "ModMail Log Viewer",
  "description": "A simple web server to view self-hosted logs",
  "repository": "https://github.com/kyb3r/logviewer",
  "env": {
    "MONGO_URI": {
        "description": "Mongodb connection URI that contains your Modmail logs.",
        "required": true
        }
    }
}
